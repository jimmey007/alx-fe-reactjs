module.exports = {
  purge: ["./src/**/*.{js,jsx,ts,tsx}", "./public/index.html"], // Paths to your files
  darkMode: false, // Or 'media'/'class' if you want dark mode
  theme: { extend: {} }, // Customize your theme here
  variants: { extend: {} }, // Extend variants if needed
  plugins: [], // Add plugins here
};